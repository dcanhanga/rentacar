export interface ICategory {
  id: string;
  name: string;
  description: string;
  created_at: Date;
}

export interface ISpecification {
  id: string;
  name: string;
  description: string;
  created_at: Date;
}
